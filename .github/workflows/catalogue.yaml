name: catalogue
on:
 workflow_dispatch:
jobs:
  catalogue:
   uses: chakra-gitactions/workflows/.github/workflows/nodejs-pipeline.yaml@main
   with:
    project: roboshop
    component: catalogue
    environment: dev
   secrets: inherit
